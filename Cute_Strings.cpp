#include <bits/stdc++.h>
using namespace std;
using ll = long long;
    
int main() {
    ios::sync_with_stdio(false);
    
    string s;
    getline(cin,s);
    if(s[0]==s[2] && s[1]=='w'){cout<<"Cute";}
    else{cout<<"No";}
    
    return 0;
}